name: Deploy via FTP/SFTP

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: '8.2'
        extensions: ctype, iconv, intl, pdo, pdo_mysql, gd, zip, dom, filter, gd, iconv, json, mbstring, pdo
        coverage: none
        
    - name: Copy .env file
      run: cp .env .env.local
      
    - name: Get Composer Cache Directory
      id: composer-cache
      run: |
        echo "dir=$(composer config cache-files-dir)" >> $GITHUB_OUTPUT
        
    - name: Cache Composer dependencies
      uses: actions/cache@v4
      with:
        path: ${{ steps.composer-cache.outputs.dir }}
        key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.lock') }}
        restore-keys: |
          ${{ runner.os }}-composer-
          
    - name: Install Composer dependencies
      run: composer install --no-dev --optimize-autoloader --no-progress --no-interaction || echo "Composer install failed"
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'yarn'
        
    - name: Check Node.js version
      run: node --version && npm --version
      
    - name: Install Yarn dependencies
      run: yarn install --frozen-lockfile --production || echo "Yarn install failed"
      
    - name: Check if yarn.lock exists
      run: ls -la yarn.lock || echo "yarn.lock not found"
      
    - name: Build assets
      run: yarn build || echo "Asset build failed"
      
    - name: Check Symfony console
      run: php bin/console --version || echo "Symfony console not found"
      
    - name: Clear Symfony cache
      run: php bin/console cache:clear --env=prod --no-warmup || echo "Cache clear failed"
      
    - name: Warmup Symfony cache
      run: php bin/console cache:warmup --env=prod || echo "Cache warmup failed"
      
    - name: Create deployment package
      run: |
        echo "Starting deployment package creation..."
        # Remove unnecessary files
        rm -rf .git .github node_modules tests .env.dev .env.test
        rm -f package-lock.json yarn.lock phpunit.dist.xml compose.yaml
        echo "Cleanup completed"
        
    - name: Upload deployment package
      uses: actions/upload-artifact@v4
      with:
        name: deployment-package
        path: .
        retention-days: 1

  deploy:
    needs: build
    runs-on: ubuntu-latest
    
    steps:
    - name: Download deployment package
      uses: actions/download-artifact@v4
      with:
        name: deployment-package
        
    - name: Deploy via FTP
      uses: SamKirkland/FTP-Deploy-Action@4.3.0
      with:
        server: ${{ secrets.FTP_SERVER }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        port: ${{ secrets.FTP_PORT || 21 }}
        local-dir: '.'
        server-dir: ${{ secrets.FTP_REMOTE_DIR || '/' }}
        exclude: ''
