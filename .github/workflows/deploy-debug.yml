name: Deploy Debug Test

on:
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  debug:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Test FTP Connection
      run: |
        echo "=== FTP DEBUG INFO ==="
        echo "Server: '${{ secrets.FTP_SERVER }}'"
        echo "Username: ${{ secrets.FTP_USERNAME }}"
        echo "Remote Dir: '${{ secrets.FTP_REMOTE_DIR }}'"
        echo "===================="
        
        # Test with completely hardcoded values
        echo "Testing with hardcoded values..."
        curl -v "ftp://ch64989:password@buezu09.ch/" \
          --list-only || echo "HARDCODED FAILED"
