name: Deploy Debug Test

on:
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  debug:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Test FTP Connection
      run: |
        echo "=== FTP DEBUG INFO ==="
        echo "Server: ${{ secrets.FTP_SERVER }}"
        echo "Username: ${{ secrets.FTP_USERNAME }}"
        echo "Remote Dir: ${{ secrets.FTP_REMOTE_DIR }}"
        echo "===================="
        
        # Test connection without port secret
        echo "Testing FTP connection without port..."
        curl -v "ftp://${{ secrets.FTP_USERNAME }}:${{ secrets.FTP_PASSWORD }}@${{ secrets.FTP_SERVER }}${{ secrets.FTP_REMOTE_DIR }}" \
          --list-only || echo "CONNECTION FAILED without port"
        
        echo "Local files:"
        ls -la
        
        # Create test file
        echo "test deployment $(date)" > test-deployment.txt
        
        echo "Uploading test file without port..."
        curl -v -T test-deployment.txt \
          "ftp://${{ secrets.FTP_USERNAME }}:${{ secrets.FTP_PASSWORD }}@${{ secrets.FTP_SERVER }}${{ secrets.FTP_REMOTE_DIR }}test-deployment.txt" \
          --ftp-create-dirs || echo "UPLOAD FAILED without port"
        
        echo "Verifying test file without port..."
        curl -v "ftp://${{ secrets.FTP_USERNAME }}:${{ secrets.FTP_PASSWORD }}@${{ secrets.FTP_SERVER }}${{ secrets.FTP_REMOTE_DIR }}" \
          --list-only || echo "VERIFICATION FAILED without port"
